services:
  serge:
    image: ghcr.io/serge-chat/serge:0.9.0
    container_name: serge
    restart: unless-stopped
    ports:
      - 8008:8008
    volumes:
      - {{CONFIG}}/serge/weights:/usr/src/app/weights
      - {{CONFIG}}/serge/db:/data/db/