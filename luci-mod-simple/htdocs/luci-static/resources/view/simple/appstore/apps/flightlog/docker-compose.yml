services:
  flightlog:
    container_name: flightlog
    restart: unless-stopped
    image: docker.io/perdian/flightlog:v2.1.11
    environment:
      - FLIGHTLOG_SERVER_CONTEXT_PATH=/
    volumes:
      - {{CONFIG}}/flightlog/db:/var/flightlog/database
    ports:
      - 8934:8080
