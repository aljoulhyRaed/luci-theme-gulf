services:
  moneroblock:
    image: docker.io/sethsimmons/moneroblock:v0.1.2
    restart: unless-stopped
    container_name: moneroblock
    ports:
      - 31312:31312
    command:
      - "--daemon"
      - "${DAEMON_ADDRESS:-node.sethforprivacy.com:18089}"