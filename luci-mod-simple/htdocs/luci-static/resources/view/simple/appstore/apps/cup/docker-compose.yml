services:
  cup:
    container_name: cup
    image: ghcr.io/sergi0g/cup:v3.5.1
    restart: unless-stopped
    command: serve
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    ports:
      - 8465:8000
